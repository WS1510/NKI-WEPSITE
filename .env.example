# Example environment variables for NKI-WEPSITE
# Do NOT put real secrets here. Replace placeholders in production with secure secrets store.

# Server
PORT=3000
NODE_ENV=production

# Database (example format)
# DATABASE_URL=postgres://user:password@host:5432/dbname
DATABASE_URL=

# SMTP (use transactional email service or SMTP credentials)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-smtp-user
SMTP_PASS=your-smtp-pass
MAIL_FROM=no-reply@example.com
SALES_EMAIL=sales@example.com

# AWS S3 (optional for attachments)
S3_BUCKET=your-bucket-name
S3_REGION=your-region
S3_ACCESS_KEY=AKIA...EXAMPLE
S3_SECRET_KEY=your-secret-key
S3_PUBLIC_URL_TEMPLATE=https://your-bucket.s3.amazonaws.com/{key}
